# Vibe Matching Reports

Reports in this directory are generated from synthetic-only fixtures unless a future rights review explicitly expands the source set.

Current rules:

- No raw private chats.
- No raw external dataset rows.
- No provider responses.
- No model binaries, vectors, checkpoints, or embeddings.
- Synthetic-only metrics are harness checks and do not support public model-quality claims.
