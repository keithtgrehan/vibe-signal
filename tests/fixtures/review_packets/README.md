# Review Packet Fixtures

This directory is reserved for synthetic review-packet inputs and expected-output snippets. Current review packet tests build outputs from `tests/fixtures/evidence_objects/valid_evidence_objects.jsonl` to avoid committing generated reviewer files.
