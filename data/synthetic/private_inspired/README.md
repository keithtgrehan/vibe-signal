# Private-Inspired Synthetic Dynamics Fixtures

These JSONL fixtures are synthetic examples generated from fixed templates and aggregate cue weights only.

They are not copied from private chats and must pass the raw-content scanner before staging.
